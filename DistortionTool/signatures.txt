#returns image
def lighten(img, value)

#returns image
def darken(img, value)

#Knocks out some pixels
#returns image
def knock_out(img, knock_scale)

#returns image
#Deteriorates image quality
def rescale(img, coef=1.3)

#returns image
def blur(img, shape=(3, 3))

#returns image
def motion_blur(img, direction)

#returns image
def sharpen(img, sharpen_amount=3, blur_shape = (3, 3))

#returns image
def add_gradient(img, xvalue=20, yvalue=20, x_inverse=False, y_inverse=False)

#Returns mask of glare
def get_glare(size, value)

#returns image
def add_glares(img, number_of_glares=2, size_range=(5., 20.), value_range=(-40, 40))

#returns image   
def add_lines(img, corner_size, prob=0.3, color=0, thickness=1)

#returns image
def spoil(img, max_noise_sigma=0.01, max_rescale=1.3, max_knock=4.,
          max_blur=2, max_motion_blur=2, max_gradient=60,
          glare_size_range=(5., 20.))

################################
#Geometric transformations
################################

#returns image
def add_crease(img, width=200, depth=10, vertical=True, inverse=False) 

#returns image
def crease_transform(img, width_range=(100, 200))

#returns image
def projective_transform(img, corner_size, corner_shift=(0, 0))
    
#returns image
def expand(img, coef=0.15, background=None)

#returns image
def partial_stretching(img, vertical=False, start=None, end=None, rel_start=None, rel_end=None,  coef=1.)

#returns image
def geometric_transform(img, word_data_list, background=None, resize_coef=0.15)

#returns image
def color_scale(img, color, color_diff)

#returns image
#This is for grey + alpha images
def grey_blit(src, dst, blend_mode="normal") 
